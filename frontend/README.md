# ReceiptGen Frontend

This is a Next.js + TypeScript frontend configured with Tailwind CSS.

Commands:
- npm run dev
- npm run build
- npm start

The home page has a Receipt form that posts to `http://localhost:5000/api/receipts`.
